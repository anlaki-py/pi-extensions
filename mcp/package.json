{
  "name": "pi-extension-mcp",
  "private": true,
  "version": "1.0.0",
  "description": "MCP (Model Context Protocol) server integration for pi",
  "type": "module",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'nothing to check'"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}